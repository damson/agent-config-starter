# Preferences

<!--
    A companion to user-dev/CLAUDE.md, symlinked to ~/.claude/preferences.md.

    IMPORTANT, and the reason this file is nearly empty: Claude Code auto-loads
    ~/.claude/CLAUDE.md and does NOT auto-load ~/.claude/preferences.md. Anything
    you put here is installed and health-checked but never read by Claude Code.

    That makes it a trap: a rule kept only here looks configured and is inert.
    Put rules that must apply in CLAUDE.md. Keep this file for reference material
    you look up yourself, or for other tools that do read it.
-->

## Reference tables

Material you consult rather than material the agent must obey. An estimation
scale, a naming cheat-sheet, a mapping you look up — anything whose absence from
the agent's context costs nothing.
